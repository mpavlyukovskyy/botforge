fleet:
  server: acemagic
  ssh_host: acemagic
  ssh_user: m
  base_dir: /opt/botforge/bots

bots:
  kristina:
    config: bots/kristina.yaml
    service: botforge-kristina
    port: 8087
  hali99:
    config: bots/hali99.yaml
    service: botforge-hali99
    port: 8089
  atlas:
    config: bots/atlas.yaml
    service: botforge-atlas
    port: 8086
  maia:
    config: bots/maia.yaml
    service: botforge-maia
    port: 3100
  # harry: TOMBSTONED 2026-06-12 — the live Harry is the standalone lp-bot
  # service (dev/bots/nzvc-lp-bot → acemagic /opt/lp-bot). This port was a
  # stale May-10 mirror and was never deployed. Do NOT resurrect without the
  # trust-rebuild plan's S28 decision gate (nzvc-lp-bot
  # docs/trust-rebuild-2026-06-12/PLAN.md). Config kept at
  # bots/harry.yaml.DORMANT.
  # harry:
  #   config: bots/harry.yaml
  #   service: botforge-harry
  #   port: 8082
  babushka:
    config: bots/babushka.yaml
    service: botforge-babushka
    port: 8085
  chief-of-staff:
    config: bots/chief-of-staff.yaml
    service: botforge-chief-of-staff
    port: 8091
  trainer:
    config: bots/trainer.yaml
    service: botforge-trainer
    port: 8092
  cleancloud:
    config: bots/cleancloud.yaml
    service: botforge-cleancloud
    port: 8093
  test-echo:
    config: bots/test-echo.yaml
    port: 9001
  test-claude:
    config: bots/test-claude.yaml
    port: 9002
  test-full:
    config: bots/test-full.yaml
    port: 9003
